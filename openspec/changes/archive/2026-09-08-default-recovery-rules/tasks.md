## 1. Resolve default and user rules

- [x] 1.1 Adapt the configuration cases in `test/retry.test.ts` to the delta spec: the exact 10+1 default snapshot, no file, `{}`, partial objects, default append, empty arrays, explicit false, clearing with user rules, flag-only clearing, string normalization, and invalid recognized values. Reuse a small table rather than a new test framework; verify the changed expectations fail against the old loader before implementation.
- [x] 1.2 Update the built-in constants and `loadConfig` in `src/retry.ts` to satisfy those cases while retaining the resolved two-array `RetryConfig` and all-or-nothing fail-closed handling. Verify the configuration cases pass, defaults are not mutated across loads, and no live user configuration or Pi setting is written.
- [x] 1.3 Retain or extend the existing recovery tests to exercise loaded defaults, explicit clearing, compact/include overlap, duplicate user/default matches, disabled ordinary retry, original-error preservation, native-recognized errors, idempotence, and protected failures. Verify eligible default input-limit errors normalize for native overflow, retry hints remain conditional, and protection tests pass without adding a recovery loop or bypassing Pi settings.

## 2. Migrate documentation to the verified behavior

- [x] 2.1 After the behavior checks pass, revise `README.md` to explain 10+1 defaults, append semantics, `clearDefaults`, valid partial objects, fail-closed errors, and the old replacement-to-explicit-clearing migration. Preserve accurate install/reload/settings/SDK/development guidance; verify each configuration example against the loader tests and each documented command against `package.json` or the existing Pi integration.
- [x] 2.2 Obtain authorization for main-spec synchronization and synchronize the verified delta with `/openspec-sync-specs default-recovery-rules`. Verify `openspec/specs/error-recovery/spec.md` exists, has a meaningful Purpose and the complete accepted requirements, and passes `openspec validate error-recovery --type spec --strict --no-interactive`; do not archive the change as part of this step.
- [x] 2.3 Add the README link to the main spec and reconcile both legacy `PLAN/` files using the mapping in `design.md`. Verify every durable agreement is preserved or explicitly superseded, obsolete checkboxes were not treated as proof, links resolve, and no current documentation still presents the old defaults or implicit replacement as active behavior.
- [x] 2.4 After migration is verified, obtain explicit confirmation to delete `PLAN/retry-includes.md` and `PLAN/overflow-compaction.md`, then remove only those files and the directory if empty. Verify their content has its documented destination and no live reference points to the removed paths. If confirmation is withheld, leave this task pending and report cleanup as deferred.

## 3. Verify the complete delivery

- [x] 3.1 Run `npm run check`, `git diff --check`, and `openspec validate --all --strict --no-interactive`. Verify all commands succeed and capture their actual results; do not infer test success or implementation completion from OpenSpec artifact status.
- [x] 3.2 Review the final code/tests, README, main spec, and change artifacts together against the delta's requirements and `design.md` migration map. Verify defaults and examples agree, unchanged safety behavior remains covered, no documentation falsely claims a release/install or completed historical work, and the user receives the evidence for acceptance. Leave publishing, installation, Git operations, and archiving to separate requests. Completed by solo self-review at the user's explicit request; independent subagent review was not performed.
